# ═══════════════════════════════════════════════════════════════════
#  Placement Analysis — Requirements  (Streamlit Cloud Safe)
#  Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════

# ── Core Framework ─────────────────────────────────────────────────
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0

# ── Machine Learning ───────────────────────────────────────────────
scikit-learn>=1.3.0
joblib>=1.3.0

# ── Data Visualisation ─────────────────────────────────────────────
plotly>=5.17.0

# ── Resume Parsing — PDF ───────────────────────────────────────────
PyPDF2>=3.0.0
pdfplumber>=0.10.0
PyMuPDF>=1.24.0

# ── Resume Parsing — DOCX ─────────────────────────────────────────
python-docx>=1.1.0

# ── Resume Parsing — Images ───────────────────────────────────────
Pillow>=10.0.0
pytesseract>=0.3.10
# NOTE: Tesseract binary is NOT available on Streamlit Cloud
# OCR will silently fall back — PDF/DOCX parsing still works fine

# ── Excel Support ─────────────────────────────────────────────────
openpyxl>=3.1.0
xlrd>=2.0.1

# ── AI — Cloud APIs (no GPU needed) ───────────────────────────────
anthropic>=0.28.0
openai>=1.30.0

# ── Security ──────────────────────────────────────────────────────
bcrypt>=4.1.0

# ── Dev / UX ──────────────────────────────────────────────────────
watchdog>=3.0.0

# ── REMOVED (causes Streamlit Cloud crash): ────────────────────────
# transformers  ← 2GB+ download, no GPU on cloud, crashes on startup
# torch         ← same issue
# accelerate    ← only needed with torch
# sentence-transformers ← pulls in torch
# streamlit-extras, streamlit-lottie ← version conflicts on Python 3.14
